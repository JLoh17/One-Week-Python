########## VARIABLE BASICS ##########
age = 104  # Assigning a variable; case sensitive
age = age + 1  # We can update the age by doing this

# Acceptable variable names:
# variable123 (never start with a digit)
# first_name (camel case works but snake case is better for Python)

# If uppercase, normally it means a constant

# Assignment operators
# +=, -=, *=, /=, //=, **=
# does arithmetic on the right side and updates the left side
